name: Release macOS DMG

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      version:
        description: "Version or tag"
        required: false
        type: string

permissions:
  contents: write

jobs:
  build-dmg:
    runs-on: macos-14
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: console/package-lock.json

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install librsvg (for correct app icon rendering)
        run: brew install librsvg

      - name: Build console frontend
        run: |
          cd console && npm ci && npm run build

      - name: Copy console into package
        run: |
          rm -rf src/copaw/console
          mkdir -p src/copaw/console
          cp -R console/dist/* src/copaw/console/

      - name: Install Python deps and PyInstaller
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
          pip install pyinstaller

      - name: Build DMG (release + dev)
        env:
          MACOSX_DEPLOYMENT_TARGET: "10.15"
          VERSION_ARG: ${{ github.event.release.tag_name || inputs.version }}
        run: |
          if [[ -n "$VERSION_ARG" ]]; then
            bash scripts/macos/build_dmg.sh "$VERSION_ARG" --dev
          else
            bash scripts/macos/build_dmg.sh --dev
          fi

      - name: Ad-hoc sign apps and recreate DMGs (macOS compatibility)
        shell: bash
        run: |
          set -euo pipefail

          sign_app() {
            local APP_DIR="$1"
            [[ -d "$APP_DIR" ]] || return 0
            local MAIN_EXE="$APP_DIR/Contents/MacOS/$(basename "$APP_DIR" .app)"
            find "$APP_DIR" -type d -name "*.dist-info" -print0 | xargs -0 rm -rf 2>/dev/null || true
            find "$APP_DIR" -type f -name "py.typed" -delete 2>/dev/null || true
            # Sign all Mach-O binaries (including main exe) for Gatekeeper
            while IFS= read -r -d '' f; do
              if /usr/bin/file -b "$f" | /usr/bin/grep -q 'Mach-O'; then
                /usr/bin/codesign --force --sign - --timestamp=none "$f" || true
              fi
            done < <(find "$APP_DIR" -type f -print0)
            # Sign the app bundle so macOS does not show "damaged"
            /usr/bin/codesign --force --sign - --timestamp=none "$APP_DIR" || true
          }

          for APP_DIR in dist/CoPaw.app dist/CoPaw-Dev.app; do
            sign_app "$APP_DIR"
          done

          # Recreate release DMG (exclude CoPaw-Dev-*.dmg)
          RELEASE_DMG="$(ls dist/CoPaw-*.dmg 2>/dev/null | grep -v 'CoPaw-Dev-' | head -1)"
          if [[ -n "${RELEASE_DMG:-}" ]]; then
            VERSION="$(basename "$RELEASE_DMG" .dmg | sed 's/^CoPaw-//')"
            rm -f "$RELEASE_DMG"
            /usr/bin/hdiutil create -volname "CoPaw $VERSION" -srcfolder dist/CoPaw.app \
              -ov -format UDZO "$RELEASE_DMG"
          fi

          # Recreate dev DMG
          DEV_DMG="$(ls dist/CoPaw-Dev-*.dmg 2>/dev/null | head -1)"
          if [[ -n "${DEV_DMG:-}" ]]; then
            VERSION="$(basename "$DEV_DMG" .dmg | sed 's/^CoPaw-Dev-//')"
            rm -f "$DEV_DMG"
            /usr/bin/hdiutil create -volname "CoPaw Dev $VERSION" \
              -srcfolder dist/CoPaw-Dev.app -ov -format UDZO "$DEV_DMG"
          fi

      - name: Upload DMG to Release
        if: github.event_name == 'release' && github.event.action == 'published'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release upload "${{ github.event.release.tag_name }}" dist/*.dmg --clobber

      - name: Upload DMG(s) separately (manual run)
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v4
        with:
          name: CoPaw-macOS-DMG-release
          path: dist/CoPaw-*.dmg
          if-no-files-found: ignore

      - name: Upload Dev DMG separately (manual run)
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v4
        with:
          name: CoPaw-macOS-DMG-dev
          path: dist/CoPaw-Dev-*.dmg
          if-no-files-found: ignore